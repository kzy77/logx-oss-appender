1. **梳理 PR #2 代码路径** ✅：确认分片上传（阻塞 `future.get`）、占位符静默回落、丢弃路径日志泄漏与未清洗载荷路径。
2. **设计分片异步上传**：为分片上传增加可复用消费线程池的有界执行器与聚合超时（`storage.uploadTimeoutMs`，默认 30s / 30000ms）；超时/失败进入重试或回退，并更新指标。补充后端卡顿的单元测试。
3. **严格占位符校验**：占位符无默认值且缺失时立即失败；在 `applyXmlConfig` 后增加必填配置自检/测试，确保缺失凭据或端点被暴露并终止启动。
4. **加固丢弃路径诊断**：用脱敏且限频的指标日志替换原丢弃日志（不含载荷长度或队列内部信息）；补充测试。
5. **清洗出站载荷**：引入共享清洗器，在入队前裁剪/转义不安全内容并强制尺寸上限；为各框架添加对应测试。
6. **文档与示例**：在 README/示例中记录新配置（分片超时、占位符校验、清洗器默认值）。
7. **验证**：运行定向单测与模块构建（`mvn -pl logx-producer,logback-oss-appender,log4j-oss-appender,log4j2-oss-appender test`）确认行为。
