<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- OSS Appender配置 -->
    <appender name="OSS" class="org.logx.logback.LogbackOSSAppender">
        <!-- 存储配置（使用环境变量，支持MinIO/S3/OSS） -->
        <endpoint>${LOGX_OSS_ENDPOINT:-http://localhost:9000}</endpoint>
        <region>${LOGX_OSS_REGION:-ap-guangzhou}</region>
        <accessKeyId>${LOGX_OSS_ACCESS_KEY_ID:-minioadmin}</accessKeyId>
        <accessKeySecret>${LOGX_OSS_ACCESS_KEY_SECRET:-minioadmin}</accessKeySecret>
        <bucket>${LOGX_OSS_BUCKET:-logx-test-bucket}</bucket>
        <keyPrefix>${LOGX_OSS_KEY_PREFIX:-logs/}</keyPrefix>
        <ossType>${LOGX_OSS_OSS_TYPE:-S3}</ossType>

        <!-- pathStyleAccess和enableSsl根据endpoint自动识别 -->
    </appender>

    <!-- 根日志配置 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="OSS"/>
    </root>
</configuration>