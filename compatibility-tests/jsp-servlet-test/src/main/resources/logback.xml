<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- OSS Appender配置 -->
    <appender name="OSS" class="org.logx.logback.LogbackOSSAppender">
        <!-- 编码器配置 -->
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>

        <!-- 存储配置（符合PRD标准） -->
        <endpoint>https://s3.amazonaws.com</endpoint>
        <region>us-east-1</region>
        <accessKeyId>test-access-key</accessKeyId>
        <accessKeySecret>test-secret-key</accessKeySecret>
        <bucket>test-bucket</bucket>
        <keyPrefix>logs/</keyPrefix>
        <ossType>SF_OSS</ossType>

        <!-- 批处理配置（符合PRD标准） -->
        <maxBatchCount>100</maxBatchCount>
        <flushIntervalMs>5000</flushIntervalMs>

        <!-- 队列配置（符合PRD标准） -->
        <maxQueueSize>10000</maxQueueSize>
    </appender>

    <!-- 根日志配置 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="OSS"/>
    </root>
</configuration>